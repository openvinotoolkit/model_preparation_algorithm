_base_: train.yaml

name: det-ta-ssd-eval
type: DetectionEvaluator

task_adapt:
    op: REPLACE  # REPLACE | MERGE
    online_mode: false

hparams:
    dummy: 0

evaluation:
    metric: bbox
    classwise: true

